{
  "name": "discord-inline-korean-translator",
  "version": "0.5.1",
  "private": true,
  "description": "WebLLM 또는 OpenAI 호환 LLM으로 Discord 메시지를 한국어로 번역하고 문맥을 참고해 영어 작성을 돕는 브라우저 확장",
  "scripts": {
    "build": "node scripts/build.mjs all",
    "build:chromium": "node scripts/build.mjs chromium",
    "build:firefox": "node scripts/build.mjs firefox",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "npm run build:chromium && node scripts/test-composer-browser.mjs",
    "test:startup": "npm run build:chromium && node scripts/test-extension-startup.mjs",
    "test:webllm": "npm run build:chromium && node scripts/test-webllm-browser.mjs",
    "check": "node --check shared.js && node --check inline-ui.js && node --check background.js && node --check webllm/cache.mjs && node --check webllm/provider.mjs && node --check webllm/entry.mjs && node --check content.js && node --check composer-dom.js && node --check composer-ui.js && node --check composer.js && node --check options/options.js && npm test && npm run build",
    "test:models": "npm run build:chromium && node scripts/test-model-picker-browser.mjs",
    "sync:models": "node scripts/sync-model-catalog.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^26.1.0",
    "playwright": "^1.63.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "slate": "^0.126.2",
    "slate-dom": "^0.126.0",
    "slate-history": "^0.113.1",
    "slate-react": "^0.126.4"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "0.2.85"
  }
}
