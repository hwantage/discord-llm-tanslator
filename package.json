{
  "name": "discord-inline-korean-translator",
  "version": "0.2.4",
  "private": true,
  "description": "OpenAI 호환 LLM으로 Discord 웹 메시지에 클릭형 한국어 번역을 추가하는 브라우저 확장",
  "scripts": {
    "build": "node scripts/build.mjs all",
    "build:chromium": "node scripts/build.mjs chromium",
    "build:firefox": "node scripts/build.mjs firefox",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check shared.js && node --check inline-ui.js && node --check background.js && node --check content.js && node --check options/options.js && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
