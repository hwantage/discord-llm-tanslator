# 개인정보 처리 원칙

Discord 한줄 번역은 기본적으로 WebLLM을 사용하여 사용자의 브라우저 GPU에서 번역과 영어 작성을 처리합니다. WebLLM 모드의 메시지 본문·작성 의도·선택한 대화·추천 결과는 외부 서버로 전송하지 않습니다.

사용자가 Ollama / OpenAI 호환 API 모드를 선택하면 요청 처리에 필요한 텍스트를 설정한 LLM 엔드포인트로 전송합니다. WebLLM 오류 시 API 서버로 자동 전환하지 않습니다. 확장 개발자가 운영하는 중계 서버, 분석 서버 또는 추적 서비스는 사용하지 않습니다.

## 저장하는 정보

- 번역 버튼과 번역 결과의 표시 설정
- 선택한 실행 방식(WebLLM / OpenAI 호환 API)
- OpenAI 호환 API Base URL
- WebLLM 모델 ID와 API 모델 ID(각각 별도로 저장)
- 사용자가 입력한 선택적 API 키

설정은 브라우저의 확장 전용 로컬 저장소에 저장되며 동기화 저장소를 사용하지 않습니다. API 키는 별도로 암호화하지 않으므로 공용 브라우저 프로필에서는 저장하지 않는 것을 권장합니다. 번역 결과 캐시는 현재 Discord 탭의 메모리에만 존재하며 페이지를 닫으면 사라집니다.

WebLLM의 모델 가중치·토크나이저·설정·WASM 실행 파일은 확장 전용 Cache Storage에 저장됩니다. 사용자가 선택한 모델을 다운로드할 때 Hugging Face(`huggingface.co`), 파일 배포 호스트(`*.hf.co`), MLC 실행 파일의 GitHub 배포 주소(`raw.githubusercontent.com/mlc-ai/binary-mlc-llm-libs/`)에 요청을 보냅니다. 해당 호스트는 일반적인 다운로드 메타데이터(IP 주소 등)를 받을 수 있습니다. 이 요청은 고정된 모델 파일 주소에 대한 GET 요청이며 Discord 텍스트·API 키·쿠키를 포함하지 않습니다. 모델 목록은 확장에 포함되며 목록 검색·필터·저장 상태 확인에는 외부 요청이 발생하지 않습니다. 모델을 바꾸어도 이전 모델의 다운로드 파일은 보관합니다. 캐시 저장 공간 확보를 위해 `unlimitedStorage` 권한을 사용하며 확장을 제거하면 모델 캐시도 제거됩니다. 각 추론 전후에 모델의 대화 상태를 초기화하고 메시지·프롬프트를 영구 저장하지 않습니다.

Chromium에서는 `offscreen` 권한으로 숨겨진 문서를 만들고 그 안의 격리 페이지에서 모델을 실행합니다. 격리 페이지는 확장 API·API 연결 설정·외부 네트워크에 직접 접근할 수 없습니다. 추론에 필요한 요청 텍스트와 선택한 모델 파일만 로컬 메시지 채널로 전달받습니다. WASM은 저장과 실행 전에 고정된 SHA-256으로 검증합니다.

영어 작성의 한국어 초안, 추천 결과, 문맥 선택 상태는 현재 탭의 메모리에만 최대 20개 대화 단위로 보관합니다. 영구 저장소나 동기화 저장소에 기록하지 않으며 새로고침 또는 탭 종료 시 사라집니다. 스레드의 현재 로드된 메시지 본문과 화면에 표시된 작성자 정보는 문맥 선택 목록을 표시하는 데 사용합니다.

## LLM 엔드포인트로 전송하는 정보

다음 전송은 사용자가 API 연결 모드를 선택했을 때만 발생합니다.

- 사용자가 번역 버튼을 누른 메시지 본문
- 사용자가 `추천 3개 만들기` 또는 `다시 추천`을 누른 한국어 작성 내용·의도
- 영어 작성이 직접 답장인 경우 해당 원글 본문 한 개(스레드 안의 직접 답장도 동일)
- 스레드 일반 작성에서 사용자가 선택한 메시지 본문들(대화 순서대로 전달)
- 작성 유형과 문맥의 발화자를 구분하기 위한 `participant_1` 같은 임시 표기
- 번역 지시문과 설정한 모델 ID
- API 키가 설정된 경우 `Authorization: Bearer …` 인증 헤더

API 키가 비어 있으면 인증 헤더를 전송하지 않습니다. API 키가 있는 원격 엔드포인트는 HTTPS만 허용합니다. 전송된 내용의 처리와 로그 보관은 사용자가 선택한 LLM 제공자 또는 자체 서버의 정책을 따릅니다.

## 전송하지 않는 정보

- Discord 인증 토큰과 쿠키
- 사용자 ID와 작성자 이름 메타데이터(본문에 사용자가 직접 적은 이름이나 멘션은 본문의 일부로 처리)
- 서버, 채널, 메시지 ID
- 번역 대상으로 요청하거나 영어 작성의 원글·문맥으로 지정하지 않은 메시지
- 번역 기록 전체

원격 API 호스트 권한은 설정 저장 시 사용자가 선택한 호스트에 대해서만 요청합니다. 확장은 분석, 광고, 원격 수집 기능을 포함하지 않으며 API 키 값을 콘솔 로그에 기록하지 않습니다.

작성창을 열거나 참고할 대화를 선택하는 것만으로 LLM 요청이 발생하지 않습니다. 추천 요청 시 선택한 문맥과 의도만 전송하며 신규 글에는 대화 문맥을 포함하지 않습니다. 영어 작성의 진단 로그에는 요청 ID, 작성 유형, 글자 수, 문맥 개수와 오류 코드 등만 기록하고 작성 의도·문맥 본문·추천 문장을 기록하지 않습니다. 확장은 Discord 메시지를 자동으로 전송하지 않습니다.
